{
  "compilerOptions": {
    "target": "ES2020",                                /* Устанавливает версию JavaScript, в которую TypeScript будет транслироваться. ES2020 поддерживает последние возможности языка. */
    "module": "esnext",                                /* Указывает, какой модульный формат использовать для компиляции. ESNext поддерживает современные стандарты. */
    "lib": ["es2020", "dom"],                          /* Включает стандартные библиотеки ES2020 и DOM, которые необходимы для работы с современными браузерами и API. */
    "sourceMap": true,                                 /* Создаёт файлы карт исходников, которые помогают при отладке кода, связывая JavaScript с оригинальным TypeScript-кодом. */
    "outDir": "./dist",                                /* Указывает каталог, куда будет помещён скомпилированный код. */
    "rootDir": "./src",                                /* Определяет корневую папку для исходных файлов, что помогает при структуре проекта. */
    "strict": true,                                    /* Включает строгий режим, который активирует все строгие проверки типов, делая код более надёжным. */
    "esModuleInterop": true,                           /* Включает поддержку для CommonJS модулей, чтобы облегчить импорт модулей из npm. */
    "forceConsistentCasingInFileNames": true,          /* Обеспечивает единообразие в использовании регистра символов в названиях файлов. */
    "skipLibCheck": true,                              /* Пропускает проверку типов в библиотечных файлах (например, в node_modules), что может ускорить компиляцию. */
    "resolveJsonModule": true,                         /* Позволяет импортировать JSON-файлы как модули. */
    "noEmitOnError": true,                             /* Запрещает эмиссию (создание выходных файлов) в случае ошибок компиляции. */
    "removeComments": true,                            /* Удаляет комментарии из скомпилированного JavaScript-кода для уменьшения размера файлов. */
    "strictNullChecks": true,                          /* Проверяет наличие null и undefined во время компиляции, делая код более предсказуемым. */
    "moduleResolution": "node",                        /* Использует Node.js стиль резолюции модулей, что полезно для работы с npm пакетами. */
    "allowSyntheticDefaultImports": true,              /* Позволяет использовать синтетические импорты для модулей без экспортов по умолчанию. */
    "downlevelIteration": true,                        /* Упрощает работу с итераторами и позволяет использовать более новые конструкции в старых версиях JavaScript. */
    "allowJs": true,                                   /* Разрешает компилировать JavaScript файлы вместе с TypeScript, что полезно для миграции проектов с JS на TS. */
    "checkJs": false,                                  /* Отключает проверку типов в JavaScript файлах, но сохраняет возможность их компиляции. */
    "importHelpers": true,                             /* Оптимизирует код, импортируя вспомогательные функции только один раз из библиотеки tslib. */
    "newLine": "lf"                                    /* Устанавливает символ новой строки LF для кросс-платформенной совместимости. */
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.js", "src/**/*.jsx"],
  "exclude": ["node_modules", "dist"]
}